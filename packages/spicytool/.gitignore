# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env & secrets (including private HAR-import output)
.env
.env.*
!.env.example

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Per-install auth signing secret (generated at first run)
backend/data/.auth_secret

# Supabase rotated refresh token (Flybasis session mode) — a live credential
backend/data/.flybasis_refresh_token
backend/data/.flybasis_refresh_token*

# Node (flybasis-mcp)
node_modules/

# Captured HTTP archives may contain session tokens — never commit
*.har
