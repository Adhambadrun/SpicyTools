{
  "name": "@spicytools/link",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SpicyTools Link — GDS itinerary → booking links + BookWithMatrix JSON, behind a real two-factor sign-in (domain-locked to @bcflights.com).",
  "scripts": {
    "dev": "node dev-server.mjs",
    "test": "node --test test/*.test.mjs",
    "start": "node dev-server.mjs"
  },
  "dependencies": {
    "resend": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
