[build]
  command = "npm run build"
  publish = "public"

# The build emits the single-file app to public/index.html so deploys ship
# only the app itself — never the repo's screenshots and source files.
# Drag-and-drop deploy: https://app.netlify.com/drop still works with the
# generated index.html alone.
